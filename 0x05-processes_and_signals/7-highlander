#!/usr/bin/env bash
# 7-highlander script that displays "To infinity and beyond" indefinitely
# with a sleep 2 between each iteration, and handles SIGTERM signal

trap 'echo "I am invincible!!!"; exit' SIGTERM

while true; do
    echo "To infinity and beyond"
    sleep 2
done

